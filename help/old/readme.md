# **************************************************************** HR System – راهنمای راه‌اندازی (مرحله ۱)

این فایل راهنمای **مرحله‌به‌مرحله** برای راه‌اندازی پروژه HR است.  
در این مرحله فقط با **پروژه، ساختار آن و خروجی مورد انتظار** آشنا می‌شویم.

> ⚠️ نکته مهم  
> این پروژه برای اولین بار توسط شما اجرا می‌شود و فرض شده است که:
> - تجربه قبلی با FastAPI و Vue ندارید
> - برای اولین بار با IIS کار می‌کنید
> - کل پروژه را به‌صورت یک فایل فشرده `HR.zip` دریافت کرده‌اید

---

## 🎯 هدف پروژه چیست؟

این پروژه یک نمونه ساده از **سامانه منابع انسانی (HR)** است که شامل:

- یک **Backend** با FastAPI (Python)
- یک **Frontend** ساده با Vue.js
- اتصال به **SQL Server**
- استفاده از **Windows Authentication (Active Directory)**

در نهایت انتظار داریم:

- سایت در مرورگر باز شود
- نام کاربر ویندوزی در بالای صفحه نمایش داده شود
- لیست کاربران از دیتابیس در یک جدول نمایش داده شود

---

## 🧩 معماری کلی سیستم (درک مفهومی)

معماری این پروژه به صورت زیر است:

مرورگر (Browser)
|
|----> Frontend (HTML + CSS + Vue)
|
|----> Backend (FastAPI)
|
|----> SQL Server


توضیح ساده:
- مرورگر فقط با **Frontend** کار می‌کند
- Frontend برای گرفتن داده با **Backend (API)** صحبت می‌کند
- Backend داده‌ها را از **SQL Server** می‌گیرد

---

## 📁 ساختار پروژه

کل پروژه داخل یک پوشه به نام `HR` قرار دارد که محتوای آن به شکل زیر است:

HR/
│
├── backend/ ← کدهای Backend (FastAPI)
│ ├── app/
│ │ ├── main.py ← نقطه شروع Backend
│ │ ├── core/ ← تنظیمات، دیتابیس، احراز هویت، لاگ
│ │ └── modules/
│ │ └── hr/ ← ماژول HR (APIها)
│ │
│ ├── requirements.txt ← لیست کتابخانه‌های Python
│ ├── run.py ← فایل اجرای Backend
│ └── venv/ ← (وجود ندارد – باید ساخته شود)
│
├── frontend/ ← کدهای Frontend
│ ├── index.html ← صفحه اصلی سایت
│ ├── css/ ← Bootstrap، FontAwesome، CSS اختصاصی
│ ├── js/ ← Vue، Axios، منطق صفحه
│ └── assets/
│ └── fonts/ ← فونت IRANSansWeb
│
├── logs/ ← فایل‌های لاگ Backend
│
└── README.md ← این راهنما


---

## 🧠 نقش هر بخش به زبان ساده

### 🔹 backend
این بخش:
- APIها را ارائه می‌دهد
- به دیتابیس وصل می‌شود
- کاربر ویندوزی را تشخیص می‌دهد
- هیچ HTML یا صفحه‌ای نمایش نمی‌دهد

---

### 🔹 frontend
این بخش:
- همان چیزی است که کاربر در مرورگر می‌بیند
- شامل HTML، CSS و JavaScript است
- از Backend داده می‌گیرد و نمایش می‌دهد

---

### 🔹 logs
در این پوشه:
- خطاها و اتفاقات مهم Backend ثبت می‌شود
- اولین جایی است که موقع خطا باید بررسی شود

---

## 👀 خروجی مورد انتظار در پایان راه‌اندازی

وقتی همه مراحل به‌درستی انجام شوند، باید بتوانید:

1. در مرورگر آدرس سایت را باز کنید
2. بالای صفحه:
   - نام کاربر ویندوزی (یا کاربر تستی در حالت DEV) را ببینید
3. پایین صفحه:
   - جدول لیست کاربران HR را مشاهده کنید
4. هیچ خطای قرمز در صفحه یا کنسول مرورگر نبینید

نمونه ذهنی خروجی:

[ سامانه منابع انسانی ] [ 👤 m.sepahkar@eit ]

| کد ملی | نام | نام خانوادگی | جنسیت | تاریخ همکاری |
| ... | ... | ... | آقا | 1401/01/01 |
| ... | ... | ... | خانم | 1400/06/15 |



# *************************************************************** HR System – راهنمای راه‌اندازی (مرحله ۲)
## آماده‌سازی پروژه و نصب وابستگی‌ها

در این مرحله فرض می‌کنیم که:
- فایل `HR.zip` را دریافت کرده‌اید
- هنوز هیچ کاری روی پروژه انجام نداده‌اید
- اولین بار است که می‌خواهید پروژه را اجرا کنید

در پایان این مرحله:
- پروژه روی سیستم شما کپی شده است
- Virtual Environment ساخته شده است
- تمام کتابخانه‌های Python نصب شده‌اند
- پروژه آماده اجرا است (ولی هنوز اجرا نشده)

---

## 1️⃣ دریافت و استخراج پروژه

### مرحله 1: کپی فایل پروژه

1. فایل `HR.zip` را دریافت کنید
2. یک مسیر ساده و بدون فاصله انتخاب کنید، مثلاً:
C:\Projects\


3. فایل `HR.zip` را در این مسیر کپی کنید
4. روی فایل راست‌کلیک کرده و **Extract Here** یا **Extract All** را انتخاب کنید

بعد از Extract، باید ساختاری شبیه این ببینید:

C:\Projects\HR
backend
frontend
logs
README.md


> ⚠️ نکته مهم  
> از قرار دادن پروژه در مسیرهایی مثل `Desktop` یا `Downloads` خودداری کنید.

---

## 2️⃣ باز کردن Command Prompt

1. دکمه **Start** ویندوز را بزنید
2. عبارت `cmd` را تایپ کنید
3. روی **Command Prompt** کلیک کنید

سپس به مسیر backend بروید:

```bat
cd C:\Projects\HR\backend


## 3️⃣ ساخت Virtual Environment (venv)

چون پوشه venv داخل فایل ZIP نیست، باید آن را خودتان بسازید.

در همان مسیر backend دستور زیر را اجرا کنید:

python -m venv venv

اگر دستور بدون خطا اجرا شد:

یک پوشه جدید به نام venv ساخته می‌شود

ساختار باید به این شکل شود:

backend\
    venv\
    app\
    requirements.txt
    run.py

## 4️⃣ فعال‌سازی Virtual Environment

برای فعال‌سازی venv دستور زیر را بزنید:

venv\Scripts\activate

اگر موفق باشد:

ابتدای خط فرمان چیزی شبیه این می‌بینید:

(venv) C:\Projects\HR\backend>

## 5️⃣ نصب کتابخانه‌های پروژه

در حالی که venv فعال است، دستور زیر را اجرا کنید:

pip install -r requirements.txt

این دستور:

تمام کتابخانه‌های مورد نیاز پروژه را نصب می‌کند

ممکن است چند دقیقه طول بکشد

اگر نصب با موفقیت انجام شود:

خطایی نمایش داده نمی‌شود

پیام‌های نصب پشت سر هم نمایش داده می‌شوند

## 6️⃣ اگر اینترنت مشکل داشت چه کار کنیم؟

اگر هنگام نصب با خطای اینترنت مواجه شدید:

بررسی کنید که از mirror داخلی استفاده می‌کنید

یا کتابخانه‌ها به‌صورت آفلاین در اختیار شما قرار داده شده‌اند

در این صورت ممکن است دستور نصب به این شکل باشد:

pip install -r requirements.txt -i https://pypi.ir/simple

یا (در حالت آفلاین):

pip install --no-index --find-links=packages -r requirements.txt

## 7️⃣ بررسی نهایی این مرحله

در پایان این مرحله، باید شرایط زیر برقرار باشد:

پوشه venv وجود دارد

(venv) در ابتدای Command Prompt دیده می‌شود

دستور pip list اجرا می‌شود و پکیج‌ها را نشان می‌دهد

هیچ خطای قرمز یا Error در Command Prompt وجود ندارد


# ************************************************************ HR System – راهنمای راه‌اندازی (مرحله ۳)
# اجرای تستی پروژه و بررسی صحت عملکرد

در این مرحله فرض می‌کنیم که:
- مراحل ۱ و ۲ را کامل انجام داده‌اید
- پروژه در مسیر مشخص روی سیستم شما قرار دارد
- Virtual Environment ساخته و فعال شده است
- کتابخانه‌ها با موفقیت نصب شده‌اند

هدف این مرحله:
- اجرای Backend به‌صورت محلی (Test Mode)
- اطمینان از سلامت Backend
- تست APIها
- آماده شدن برای راه‌اندازی روی IIS

---

## 1️⃣ اطمینان از فعال بودن Virtual Environment

قبل از هر کاری، مطمئن شوید که Virtual Environment فعال است.

در Command Prompt باید ابتدای خط چیزی شبیه زیر ببینید:

(venv) C:\Projects\HR\backend>

اگر (venv) را نمی‌بینید، دستور زیر را اجرا کنید:

venv\Scripts\activate

---

## 2️⃣ اجرای Backend به‌صورت تستی

در همان مسیر backend دستور زیر را اجرا کنید:

python run.py

اگر Backend درست اجرا شود، باید اتفاقات زیر را ببینید:

- پیام شروع برنامه در Command Prompt
- لاگ‌هایی شبیه:
  - HR SYSTEM STARTING
  - Environment : DEV
  - Database connection OK

اگر در این مرحله خطا دیدید:
- متن خطا را یادداشت کنید
- به مرحله ۴ (خطاها) مراجعه کنید
- یا از مسئول پروژه سؤال بپرسید

---

## 3️⃣ تست Backend در مرورگر (Swagger)

مرورگر را باز کنید و آدرس زیر را وارد کنید:

http://localhost:8000/docs

اگر Backend سالم باشد:
- یک صفحه با عنوان Swagger UI باز می‌شود
- لیست APIها را می‌بینید
- می‌توانید APIها را تست کنید

اگر این صفحه باز نشد:
- Backend اجرا نشده است
- یا پورت 8000 توسط برنامه دیگری اشغال شده است

---

## 4️⃣ تست Health Check سیستم

در مرورگر آدرس زیر را وارد کنید:

http://localhost:8000/health

خروجی مورد انتظار:

{
  "status": "ok",
  "database": "connected",
  "environment": "DEV"
}

توضیح:
- status = ok  → Backend سالم است
- database = connected → اتصال دیتابیس برقرار است

اگر database برابر disconnected بود:
- تنظیمات دیتابیس در فایل .env را بررسی کنید
- مطمئن شوید SQL Server در حال اجراست

---

## 5️⃣ تست API لیست کاربران

در مرورگر یا Swagger آدرس زیر را تست کنید:

http://localhost:8000/api/hr/users

خروجی مورد انتظار:
- یک لیست (آرایه) از کاربران
- هر آیتم شامل:
  - NationalCode
  - FirstName
  - LastName
  - Gender
  - ContractDate

اگر خطا دریافت کردید:
- دیتابیس درست وصل نشده
- یا View / Table مورد نظر وجود ندارد

---

## 6️⃣ تست API کاربر جاری (DEV Mode)

در حالت DEV، کاربر از متغیر DEV_USER خوانده می‌شود.

آدرس زیر را در مرورگر باز کنید:

http://localhost:8000/api/hr/me

خروجی مورد انتظار:

{
  "username": "test.user",
  "full_name": "test.user@eit"
}

اگر این API خطا داد:
- فایل .env را بررسی کنید
- مقدار DEV_USER باید وجود داشته باشد

---

## 7️⃣ تست Frontend به‌صورت محلی

به مسیر frontend بروید:

C:\Projects\HR\frontend\

روی فایل index.html دوبار کلیک کنید (Double Click).

اگر همه چیز درست باشد:
- صفحه HR باز می‌شود
- بالای صفحه نام کاربر نمایش داده می‌شود
- جدول لیست کاربران نمایش داده می‌شود

اگر جدول خالی بود:
- Backend اجرا نیست
- یا API /api/hr/users خطا دارد

---

## 8️⃣ بررسی خطاها در مرورگر

در صفحه Frontend:
- کلید F12 را بزنید
- به تب Console بروید

نباید خطای قرمز ببینید.

اگر خطای 404 یا 500 دیدید:
- آدرس API اشتباه است
- Backend اجرا نیست
- یا CORS مشکل دارد

---

## 9️⃣ جمع‌بندی مرحله سوم

در پایان این مرحله، باید مطمئن باشید که:

- Backend بدون خطا اجرا می‌شود
- Swagger در دسترس است
- Health Check سالم است
- APIها داده برمی‌گردانند
- Frontend داده‌ها را نمایش می‌دهد

اگر حتی یکی از این موارد درست کار نکند:
- به مرحله بعد نروید
- ابتدا مشکل را برطرف کنید

---


```yaml
# *********************************************************** HR System – راهنمای راه‌اندازی (مرحله ۴)
# راه‌اندازی کامل سایت در IIS (گام‌به‌گام برای فرد مبتدی)

در این مرحله فرض می‌کنیم که:
- مراحل ۱، ۲ و ۳ را با موفقیت انجام داده‌اید
- Backend به‌صورت تستی با دستور `python run.py` اجرا شده و سالم است
- Frontend به‌صورت محلی (index.html) درست نمایش داده می‌شود
- هیچ تجربه قبلی جدی با IIS ندارید

هدف این مرحله:
- تعریف سایت Frontend در IIS
- فعال‌سازی Windows Authentication
- اتصال Frontend به Backend از طریق Reverse Proxy
- باز کردن سایت از طریق IIS در مرورگر

در پایان این مرحله:
- سایت را از طریق یک URL (مثلاً http://localhost:8080) باز می‌کنید
- نام کاربر ویندوزی نمایش داده می‌شود
- لیست کاربران نمایش داده می‌شود

---

## 1️⃣ آشنایی خیلی کوتاه با IIS (برای شروع)

IIS یک وب‌سرور ویندوزی است.
در این پروژه:
- IIS فایل‌های Frontend (HTML, CSS, JS) را سرو می‌کند
- IIS کار احراز هویت ویندوز (Active Directory) را انجام می‌دهد
- درخواست‌های `/api/*` را به Backend (FastAPI) هدایت می‌کند

به زبان ساده:
- Frontend ← IIS
- Backend ← FastAPI (روی پورت 8000)
- IIS بین این دو واسطه است

---

## 2️⃣ باز کردن IIS Manager

1. دکمه Start ویندوز را بزنید
2. عبارت `IIS` را تایپ کنید
3. روی **Internet Information Services (IIS) Manager** کلیک کنید

اگر پنجره IIS Manager باز شد، یعنی IIS روی سیستم شما نصب است.

---

## 3️⃣ ساخت سایت جدید برای Frontend

### مرحله 3-1: انتخاب Sites

در پنل سمت چپ IIS Manager:
- روی نام کامپیوتر کلیک کنید
- سپس روی پوشه **Sites** کلیک کنید

---

### مرحله 3-2: افزودن سایت جدید

1. روی **Sites** راست‌کلیک کنید
2. گزینه **Add Website…** را انتخاب کنید

پنجره‌ای باز می‌شود.

---

### مرحله 3-3: پر کردن اطلاعات سایت

در پنجره Add Website این مقادیر را وارد کنید:

- Site name:
  HR-Frontend

- Physical path:
  مسیر فولدر frontend پروژه، مثلاً:
  C:\Projects\HR\frontend

- Binding:
  - Type: http
  - IP address: All Unassigned
  - Port: 8080
  - Host name: (خالی بگذارید)

سپس روی **OK** کلیک کنید.

---

## 4️⃣ تست اولیه سایت Frontend در IIS

مرورگر را باز کنید و آدرس زیر را وارد کنید:

http://localhost:8080

اگر همه‌چیز درست باشد:
- صفحه سامانه منابع انسانی باز می‌شود
- ممکن است هنوز داده‌ها نیایند (طبیعی است)

اگر صفحه باز نشد:
- IIS Manager → Sites → HR-Frontend
- وضعیت سایت باید **Started** باشد
- اگر Stopped بود، راست‌کلیک → Start

---

## 5️⃣ فعال‌سازی Windows Authentication

### مرحله 5-1: ورود به تنظیمات Authentication

1. در IIS Manager روی سایت **HR-Frontend** کلیک کنید
2. در وسط صفحه روی **Authentication** دوبار کلیک کنید

---

### مرحله 5-2: تنظیم Authentication

در لیست Authentication:

- **Windows Authentication**
  - Right Click → Enable

- **Anonymous Authentication**
  - Right Click → Disable

این مرحله بسیار مهم است، چون:
- نام کاربر ویندوزی از اینجا به Backend منتقل می‌شود

---

## 6️⃣ نصب و فعال‌سازی Reverse Proxy (ARR)

برای اینکه IIS درخواست‌های API را به FastAPI بفرستد، نیاز به Reverse Proxy داریم.

### مرحله 6-1: نصب ARR (اگر نصب نیست)

اگر گزینه‌های ARR را ندارید:
- باید **Application Request Routing** نصب شود
- این کار معمولاً توسط مدیر سیستم انجام می‌شود

---

### مرحله 6-2: فعال‌سازی Proxy

1. در IIS Manager روی نام کامپیوتر (بالای Sites) کلیک کنید
2. در وسط صفحه روی **Application Request Routing Cache** کلیک کنید
3. در پنل سمت راست روی **Server Proxy Settings** کلیک کنید
4. گزینه **Enable Proxy** را تیک بزنید
5. روی **Apply** کلیک کنید

---

## 7️⃣ ساخت Rule برای هدایت API به FastAPI

### مرحله 7-1: ورود به URL Rewrite

1. روی سایت **HR-Frontend** کلیک کنید
2. در وسط صفحه روی **URL Rewrite** دوبار کلیک کنید
3. روی **Add Rule(s)…** کلیک کنید
4. گزینه **Reverse Proxy** را انتخاب کنید

---

### مرحله 7-2: تنظیم Reverse Proxy

در پنجره بازشده:

- Inbound Rules:
  - Enter the URL of the server:  
    http://127.0.0.1:8000

- تیک گزینه:
  - Enable SSL Offloading (اگر وجود داشت)

روی **OK** کلیک کنید.

---

### مرحله 7-3: محدود کردن به مسیر /api

پس از ساخت Rule:
- Rule ساخته‌شده را باز کنید
- شرط (Condition) را طوری تنظیم کنید که:
  فقط درخواست‌هایی که با `/api/` شروع می‌شوند هدایت شوند

به این معنا:
- http://localhost:8080/api/... → FastAPI
- بقیه مسیرها → Frontend

---

## 8️⃣ اجرای Backend در کنار IIS

در این مرحله:
- IIS فقط Frontend را اجرا می‌کند
- Backend باید جداگانه اجرا شود

در Command Prompt:

cd C:\Projects\HR\backend
venv\Scripts\activate
python run.py

این پنجره باید باز بماند.

---

## 9️⃣ تست نهایی از طریق IIS

مرورگر را باز کنید و آدرس زیر را وارد کنید:

http://localhost:8080

انتظار داریم:
- صفحه Frontend باز شود
- نام کاربر ویندوزی نمایش داده شود
- لیست کاربران نمایش داده شود

همچنین این آدرس را تست کنید:

http://localhost:8080/api/hr/me

اگر JSON کاربر را دیدید:
- Reverse Proxy درست کار می‌کند
- Authentication درست انجام شده است

---

## 10️⃣ جمع‌بندی مرحله چهارم

در پایان این مرحله:

- سایت Frontend در IIS تعریف شده است
- Windows Authentication فعال است
- درخواست‌های API به FastAPI هدایت می‌شوند
- سایت از طریق IIS در مرورگر باز می‌شود

اگر هر کدام از این موارد درست کار نکرد:
- به مرحله بعد (خطاها) مراجعه کنید
- یا از مسئول پروژه کمک بگیرید

---

## ⏭️ مرحله بعدی چیست؟

در مرحله ۵:
- خطاهای رایج IIS
- خطاهای Authentication
- خطاهای Reverse Proxy
- و روش حل آن‌ها

را به‌صورت موردی و عملی بررسی می‌کنیم.
```


# ************************************************************* HR System – راهنمای راه‌اندازی (مرحله ۴)
# راه‌اندازی کامل سایت در IIS (گام‌به‌گام برای فرد مبتدی)

در این مرحله فرض می‌کنیم که:
- مراحل ۱، ۲ و ۳ را با موفقیت انجام داده‌اید
- Backend به‌صورت تستی با دستور `python run.py` اجرا شده و سالم است
- Frontend به‌صورت محلی (index.html) درست نمایش داده می‌شود
- هیچ تجربه قبلی جدی با IIS ندارید

هدف این مرحله:
- تعریف سایت Frontend در IIS
- فعال‌سازی Windows Authentication
- اتصال Frontend به Backend از طریق Reverse Proxy
- باز کردن سایت از طریق IIS در مرورگر

در پایان این مرحله:
- سایت را از طریق یک URL (مثلاً http://localhost:8080) باز می‌کنید
- نام کاربر ویندوزی نمایش داده می‌شود
- لیست کاربران نمایش داده می‌شود

---

## 1️⃣ آشنایی خیلی کوتاه با IIS (برای شروع)

IIS یک وب‌سرور ویندوزی است.
در این پروژه:
- IIS فایل‌های Frontend (HTML, CSS, JS) را سرو می‌کند
- IIS کار احراز هویت ویندوز (Active Directory) را انجام می‌دهد
- درخواست‌های `/api/*` را به Backend (FastAPI) هدایت می‌کند

به زبان ساده:
- Frontend ← IIS
- Backend ← FastAPI (روی پورت 8000)
- IIS بین این دو واسطه است

---

## 2️⃣ باز کردن IIS Manager

1. دکمه Start ویندوز را بزنید
2. عبارت `IIS` را تایپ کنید
3. روی **Internet Information Services (IIS) Manager** کلیک کنید

اگر پنجره IIS Manager باز شد، یعنی IIS روی سیستم شما نصب است.

---

## 3️⃣ ساخت سایت جدید برای Frontend

### مرحله 3-1: انتخاب Sites

در پنل سمت چپ IIS Manager:
- روی نام کامپیوتر کلیک کنید
- سپس روی پوشه **Sites** کلیک کنید

---

### مرحله 3-2: افزودن سایت جدید

1. روی **Sites** راست‌کلیک کنید
2. گزینه **Add Website…** را انتخاب کنید

پنجره‌ای باز می‌شود.

---

### مرحله 3-3: پر کردن اطلاعات سایت

در پنجره Add Website این مقادیر را وارد کنید:

- Site name:
  HR-Frontend

- Physical path:
  مسیر فولدر frontend پروژه، مثلاً:
  C:\Projects\HR\frontend

- Binding:
  - Type: http
  - IP address: All Unassigned
  - Port: 8080
  - Host name: (خالی بگذارید)

سپس روی **OK** کلیک کنید.

---

## 4️⃣ تست اولیه سایت Frontend در IIS

مرورگر را باز کنید و آدرس زیر را وارد کنید:

http://localhost:8080

اگر همه‌چیز درست باشد:
- صفحه سامانه منابع انسانی باز می‌شود
- ممکن است هنوز داده‌ها نیایند (طبیعی است)

اگر صفحه باز نشد:
- IIS Manager → Sites → HR-Frontend
- وضعیت سایت باید **Started** باشد
- اگر Stopped بود، راست‌کلیک → Start

---

## 5️⃣ فعال‌سازی Windows Authentication

### مرحله 5-1: ورود به تنظیمات Authentication

1. در IIS Manager روی سایت **HR-Frontend** کلیک کنید
2. در وسط صفحه روی **Authentication** دوبار کلیک کنید

---

### مرحله 5-2: تنظیم Authentication

در لیست Authentication:

- **Windows Authentication**
  - Right Click → Enable

- **Anonymous Authentication**
  - Right Click → Disable

این مرحله بسیار مهم است، چون:
- نام کاربر ویندوزی از اینجا به Backend منتقل می‌شود

---

## 6️⃣ نصب و فعال‌سازی Reverse Proxy (ARR)

برای اینکه IIS درخواست‌های API را به FastAPI بفرستد، نیاز به Reverse Proxy داریم.

### مرحله 6-1: نصب ARR (اگر نصب نیست)

اگر گزینه‌های ARR را ندارید:
- باید **Application Request Routing** نصب شود
- این کار معمولاً توسط مدیر سیستم انجام می‌شود

---

### مرحله 6-2: فعال‌سازی Proxy

1. در IIS Manager روی نام کامپیوتر (بالای Sites) کلیک کنید
2. در وسط صفحه روی **Application Request Routing Cache** کلیک کنید
3. در پنل سمت راست روی **Server Proxy Settings** کلیک کنید
4. گزینه **Enable Proxy** را تیک بزنید
5. روی **Apply** کلیک کنید

---

## 7️⃣ ساخت Rule برای هدایت API به FastAPI

### مرحله 7-1: ورود به URL Rewrite

1. روی سایت **HR-Frontend** کلیک کنید
2. در وسط صفحه روی **URL Rewrite** دوبار کلیک کنید
3. روی **Add Rule(s)…** کلیک کنید
4. گزینه **Reverse Proxy** را انتخاب کنید

---

### مرحله 7-2: تنظیم Reverse Proxy

در پنجره بازشده:

- Inbound Rules:
  - Enter the URL of the server:  
    http://127.0.0.1:8000

- تیک گزینه:
  - Enable SSL Offloading (اگر وجود داشت)

روی **OK** کلیک کنید.

---

### مرحله 7-3: محدود کردن به مسیر /api

پس از ساخت Rule:
- Rule ساخته‌شده را باز کنید
- شرط (Condition) را طوری تنظیم کنید که:
  فقط درخواست‌هایی که با `/api/` شروع می‌شوند هدایت شوند

به این معنا:
- http://localhost:8080/api/... → FastAPI
- بقیه مسیرها → Frontend

---

## 8️⃣ اجرای Backend در کنار IIS

در این مرحله:
- IIS فقط Frontend را اجرا می‌کند
- Backend باید جداگانه اجرا شود

در Command Prompt:

cd C:\Projects\HR\backend
venv\Scripts\activate
python run.py

این پنجره باید باز بماند.

---

## 9️⃣ تست نهایی از طریق IIS

مرورگر را باز کنید و آدرس زیر را وارد کنید:

http://localhost:8080

انتظار داریم:
- صفحه Frontend باز شود
- نام کاربر ویندوزی نمایش داده شود
- لیست کاربران نمایش داده شود

همچنین این آدرس را تست کنید:

http://localhost:8080/api/hr/me

اگر JSON کاربر را دیدید:
- Reverse Proxy درست کار می‌کند
- Authentication درست انجام شده است

---

## 10️⃣ جمع‌بندی مرحله چهارم

در پایان این مرحله:

- سایت Frontend در IIS تعریف شده است
- Windows Authentication فعال است
- درخواست‌های API به FastAPI هدایت می‌شوند
- سایت از طریق IIS در مرورگر باز می‌شود

اگر هر کدام از این موارد درست کار نکرد:
- به مرحله بعد (خطاها) مراجعه کنید
- یا از مسئول پروژه کمک بگیرید

---


# ****************************************************************** HR System – راهنمای راه‌اندازی (مرحله ۵)
# خطاهای رایج و روش برطرف کردن آن‌ها (Troubleshooting)

در این مرحله فرض می‌کنیم که:
- مراحل ۱ تا ۴ را انجام داده‌اید
- پروژه حداقل یک‌بار بالا آمده یا تلاش کرده‌اید آن را بالا بیاورید
- ممکن است با خطاهای مختلفی روبه‌رو شده باشید

هدف این مرحله:
- آشنایی با خطاهای رایج
- دانستن علت هر خطا
- یاد گرفتن روش دقیق برطرف کردن آن

این مرحله را با دقت بخوانید؛
بیشتر مشکلات واقعی پروژه‌ها دقیقاً همین‌جا حل می‌شوند.

---

## ❌ خطای ۱: Backend اصلاً بالا نمی‌آید (python run.py خطا می‌دهد)

### نشانه‌ها:
- بعد از اجرای `python run.py` برنامه فوراً متوقف می‌شود
- خطای قرمز در Command Prompt نمایش داده می‌شود

### دلایل رایج:
- Virtual Environment فعال نیست
- کتابخانه‌ها نصب نشده‌اند
- نسخه Python اشتباه است

### راه‌حل:
1. مطمئن شوید `(venv)` در ابتدای خط فرمان دیده می‌شود
2. اگر نیست:
   venv\Scripts\activate
3. کتابخانه‌ها را دوباره نصب کنید:
   pip install -r requirements.txt
4. نسخه Python را چک کنید:
   python --version
   (باید 3.10 یا بالاتر باشد)

---

## ❌ خطای ۲: خطای اتصال به دیتابیس (Database connection FAILED)

### نشانه‌ها:
- در لاگ Startup پیام:
  Database connection FAILED
- یا Health Check مقدار:
  database: disconnected

### دلایل رایج:
- اطلاعات دیتابیس در فایل `.env` اشتباه است
- SQL Server اجرا نشده است
- ODBC Driver نصب نیست

### راه‌حل:
1. فایل `.env` را باز کنید و این موارد را بررسی کنید:
   - DB_NAME
   - DB_USER
   - DB_PASSWORD
   - DB_HOST
   - DB_PORT
2. مطمئن شوید SQL Server در حال اجراست
3. بررسی کنید ODBC Driver نصب است:
   Control Panel → ODBC Data Sources → Drivers
4. اگر Driver وجود ندارد، باید نصب شود

---

## ❌ خطای ۳: آدرس http://localhost:8000/docs باز نمی‌شود

### نشانه‌ها:
- مرورگر خطای Page Not Found یا Connection Refused می‌دهد

### دلایل رایج:
- Backend اجرا نشده است
- پورت 8000 توسط برنامه دیگری اشغال است

### راه‌حل:
1. مطمئن شوید Command Prompt هنوز باز است و `run.py` در حال اجراست
2. اگر پورت اشغال است:
   - برنامه دیگری که از پورت 8000 استفاده می‌کند را ببندید
   - یا پورت را در `run.py` تغییر دهید

---

## ❌ خطای ۴: Frontend باز می‌شود ولی جدول کاربران خالی است

### نشانه‌ها:
- صفحه سایت بالا می‌آید
- جدول نمایش داده می‌شود
- اما هیچ داده‌ای در جدول نیست

### دلایل رایج:
- Backend اجرا نیست
- API `/api/hr/users` خطا می‌دهد
- Reverse Proxy درست تنظیم نشده

### راه‌حل:
1. این آدرس را مستقیم تست کنید:
   http://localhost:8000/api/hr/users
2. اگر خطا دارد:
   - مشکل Backend یا دیتابیس است
3. اگر درست است ولی در IIS کار نمی‌کند:
   - Reverse Proxy را بررسی کنید
   - مسیر `/api/*` باید به FastAPI هدایت شود

---

## ❌ خطای ۵: نام کاربر ویندوزی نمایش داده نمی‌شود

### نشانه‌ها:
- در بالای صفحه نوشته شده:
  کاربر ناشناس
- یا مقدار خالی نمایش داده می‌شود

### دلایل رایج:
- Windows Authentication فعال نیست
- Anonymous Authentication هنوز فعال است
- API `/api/hr/me` کار نمی‌کند

### راه‌حل:
1. در IIS:
   - Windows Authentication → Enable
   - Anonymous Authentication → Disable
2. آدرس زیر را تست کنید:
   http://localhost:8080/api/hr/me
3. اگر در حالت DEV هستید:
   - مقدار DEV_USER در `.env` باید تنظیم شده باشد

---

## ❌ خطای ۶: خطای 401 یا 403 هنگام فراخوانی API

### نشانه‌ها:
- مرورگر یا Console خطای 401 / 403 نمایش می‌دهد

### دلایل رایج:
- Windows Authentication درست تنظیم نشده
- مرورگر احراز هویت نشده
- IIS اجازه دسترسی نمی‌دهد

### راه‌حل:
1. سایت را با مرورگر Edge یا Chrome باز کنید
2. مطمئن شوید با کاربر دامنه لاگین هستید
3. تنظیمات Authentication سایت در IIS را دوباره بررسی کنید

---

## ❌ خطای ۷: خطای CORS در Console مرورگر

### نشانه‌ها:
- خطای CORS در Console
- درخواست API بلاک می‌شود

### دلایل رایج:
- Backend از دامنه IIS درخواست را قبول نمی‌کند

### راه‌حل:
- در `main.py` بررسی کنید که CORS Middleware فعال است
- در حالت DEV، allow_origins روی "*" باشد

---

## ❌ خطای ۸: فونت فارسی اعمال نمی‌شود

### نشانه‌ها:
- متن‌ها با فونت پیش‌فرض نمایش داده می‌شوند

### دلایل رایج:
- مسیر فونت‌ها اشتباه است
- فایل فونت در IIS سرو نمی‌شود

### راه‌حل:
1. مسیر فونت‌ها را بررسی کنید:
   frontend/assets/fonts/woff2/
2. در DevTools مرورگر:
   - Network → Font
   - بررسی کنید فونت 404 نباشد
3. در IIS مطمئن شوید Static Content فعال است

---

## ❌ خطای ۹: سایت IIS بالا نمی‌آید (404 یا 500)

### نشانه‌ها:
- http://localhost:8080 باز نمی‌شود
- خطای IIS نمایش داده می‌شود

### دلایل رایج:
- Physical Path اشتباه است
- سایت Start نشده
- پورت اشغال است

### راه‌حل:
1. IIS Manager → Sites → HR-Frontend
2. وضعیت سایت باید Started باشد
3. Physical Path باید دقیقاً مسیر frontend باشد
4. پورت انتخاب‌شده آزاد باشد

---

## ❌ خطای ۱۰: هیچ لاگی نداریم و نمی‌دانیم چه شده

### راه‌حل حرفه‌ای:
1. فایل زیر را باز کنید:
   logs/app.log
2. جدیدترین خطاها را از پایین به بالا بخوانید
3. پیام خطا معمولاً دقیقاً علت را می‌گوید

---

## 🧠 توصیه نهایی (خیلی مهم)

وقتی به مشکل خوردید:
1. وحشت نکنید
2. خطا را کامل بخوانید
3. لاگ را بررسی کنید
4. API را مستقیم تست کنید
5. فقط بعد از این‌ها سؤال بپرسید

---

## ✅ پایان راهنما

اگر تمام مراحل ۱ تا ۵ را انجام داده باشید:
- پروژه HR روی IIS اجرا می‌شود
- Backend و Frontend به هم وصل هستند
- احراز هویت ویندوزی کار می‌کند
- شما یک سیستم واقعی سازمانی را بالا آورده‌اید

تبریک 🎉
شما اولین پروژه FastAPI + Vue + IIS خود را اجرا کرده‌اید.
